" avoid annoying escape key
inoremap jj <Esc>

" press enter for new line in command mode
nmap <CR> O<Esc>j

" emacs-like tabs
set tabstop=4
set autoindent
set expandtab
set softtabstop=4
set shiftwidth=4

" make autocomplete more like bash
set wildmode=longest,list,full
set wildmenu

" wrap lines
